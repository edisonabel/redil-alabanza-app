---
import Layout from '../layouts/Layout.astro';
import OnboardingRoot from '../components/onboarding/OnboardingRoot';
import BotonNotificaciones from '../components/react/BotonNotificaciones.jsx';
import GoogleCalendarConnection from '../components/react/GoogleCalendarConnection.jsx';
import { VOCAL_RANGE_OPTIONS } from '../lib/ministry-config.js';

const userId = Astro.locals.user?.id ?? null;
---

<Layout title="Mi Perfil - Redil">
 <main class="profile-wizard-shell">
 <article class="profile-wizard-panel" aria-labelledby="profile-page-title">
 <header class="profile-wizard-hero" data-tour="perfil-photo">
 <div class="profile-wizard-hero-backdrop" id="profile-hero-backdrop" aria-hidden="true">
 <img id="profile-hero-backdrop-image" alt="" crossorigin="anonymous" />
 </div>
 <div class="profile-wizard-hero-glow" aria-hidden="true"></div>
 <div class="profile-wizard-portrait-wrap">
 <div id="avatar-container" class="profile-wizard-portrait bg-neutral/20 text-content-muted">
 <svg class="w-8 h-8 animate-pulse" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
 </div>
 </div>
 <button id="btn-change-avatar" class="profile-wizard-photo-button" type="button" title="Cambiar foto" aria-label="Cambiar foto de perfil" disabled>
 <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14.5 4 16 6h3a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h3l1.5-2h5Z"/><circle cx="12" cy="13" r="3"/></svg>
 <span>Cambiar foto</span>
 </button>
 <input type="file" id="avatar-input" accept="image/*" class="hidden" />

 <div class="profile-wizard-identity">
 <p class="profile-wizard-eyebrow">Mi perfil</p>
 <h1 id="profile-page-title" class="sr-only">Mi Perfil</h1>
 <h2 id="ui-user-name" class="h-8 min-w-[140px] animate-pulse rounded-lg bg-white/15"></h2>
 <p id="ui-user-email" class="h-5 min-w-[180px] animate-pulse rounded-lg bg-white/10"></p>
 <div id="ui-roles-container" class="profile-wizard-roles">
 <div class="h-6 w-16 animate-pulse rounded-full bg-white/10"></div>
 <div class="h-6 w-20 animate-pulse rounded-full bg-white/10"></div>
 </div>
 </div>
 </header>

 <nav class="profile-wizard-nav" aria-label="Opciones de Mi Perfil" role="tablist">
 <button type="button" class="profile-wizard-nav-item" data-profile-step="0" role="tab" aria-selected="true" aria-controls="profile-step-data">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
 <span>Datos</span>
 </button>
 <button type="button" class="profile-wizard-nav-item" data-profile-step="1" role="tab" aria-selected="false" aria-controls="profile-step-voice" tabindex="-1">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><rect x="9" y="3" width="6" height="11" rx="3"/><path d="M5.5 11a6.5 6.5 0 0 0 13 0M12 17.5V21M9 21h6"/></svg>
 <span>Voz</span>
 </button>
 <button type="button" class="profile-wizard-nav-item" data-profile-step="2" role="tab" aria-selected="false" aria-controls="profile-step-agenda" tabindex="-1">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></svg>
 <span>Agenda</span>
 </button>
 <button type="button" class="profile-wizard-nav-item" data-profile-step="3" role="tab" aria-selected="false" aria-controls="profile-step-account" tabindex="-1">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><path d="M12 3 4 7v5c0 5 3.4 8.3 8 9 4.6-.7 8-4 8-9V7l-8-4Z"/><path d="M9 12l2 2 4-4"/></svg>
 <span>Cuenta</span>
 </button>
 </nav>

 <div class="profile-wizard-stage" id="profile-wizard-stage">
 <section id="profile-step-data" class="profile-wizard-step" data-profile-panel="0" data-state="active" role="tabpanel">
 <div class="profile-wizard-step-heading">
 <h3>Datos personales</h3>
 </div>
 <form id="form-datos-personales" class="profile-form-grid">
 <label class="profile-field">
 <span>Nombre completo</span>
 <input type="text" id="input-nombre" disabled placeholder="Cargando..." />
 </label>
 <label class="profile-field">
 <span>Correo de acceso</span>
 <input type="email" id="input-correo" required autocomplete="email" inputmode="email" disabled placeholder="Cargando..." />
 </label>
 <label class="profile-field" data-tour="perfil-birthday">
 <span>Fecha de nacimiento</span>
 <input type="date" id="input-cumpleanos" required disabled />
 </label>
 <label class="profile-field">
 <span>Tel&eacute;fono / WhatsApp</span>
 <input type="tel" id="input-telefono" disabled placeholder="Ej: +123456789" />
 </label>
 </form>
 </section>

 <section id="profile-step-voice" class="profile-wizard-step" data-profile-panel="1" data-state="after" role="tabpanel" aria-hidden="true" inert>
 <div class="profile-wizard-step-heading">
 <h3>Voz</h3>
 </div>
 <div class="profile-voice-card">
 <label class="profile-field profile-voice-field" for="tonalidad-select">
 <span>Tipo de voz</span>
 <select id="tonalidad-select" form="form-datos-personales" disabled>
 <option value="">No soy voz / Todav&iacute;a no lo s&eacute;</option>
 {VOCAL_RANGE_OPTIONS.map((option) => (
 <option value={option.value}>{option.label}</option>
 ))}
 </select>
 </label>
 </div>
 </section>

 <section id="profile-step-agenda" class="profile-wizard-step" data-profile-panel="2" data-state="after" role="tabpanel" aria-hidden="true" inert>
 <div class="profile-wizard-step-heading">
 <h3>Agenda</h3>
 </div>
 <div class="profile-agenda-stack">
 <div class="profile-calendar-slot">
 <GoogleCalendarConnection client:load />
 </div>
 <section class="profile-content-card profile-absence-section" data-tour="perfil-availability">
 <div class="profile-content-card-heading">
 <span class="profile-feature-icon profile-feature-icon--agenda" aria-hidden="true">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 10h18"/></svg>
 </span>
 <div>
 <h4>Mis ausencias</h4>
 </div>
 </div>
 <form id="form-ausencia" class="profile-absence-form opacity-50 pointer-events-none transition-opacity">
 <label class="profile-field">
 <span>Fecha inicio</span>
 <input type="date" id="ausencia-inicio" required />
 </label>
 <label class="profile-field">
 <span>Fecha fin</span>
 <input type="date" id="ausencia-fin" required />
 </label>
 <label class="profile-field profile-absence-reason">
 <span>Motivo <em>(opcional)</em></span>
 <div class="profile-absence-action-row">
 <input type="text" id="ausencia-motivo" placeholder="Ej. Viaje familiar, trabajo..." />
 <button type="submit" id="btn-save-ausencia">Registrar</button>
 </div>
 </label>
 </form>
 <p class="profile-content-note">M&iacute;nimo 7 d&iacute;as. Las asignaciones coincidentes se retirar&aacute;n autom&aacute;ticamente.</p>
 <div class="space-y-3" id="ausencias-list">
 <div class="profile-empty-state">
 <p>Cargando ausencias...</p>
 </div>
 </div>
 </section>
 </div>
 </section>

 <section id="profile-step-account" class="profile-wizard-step" data-profile-panel="3" data-state="after" role="tabpanel" aria-hidden="true" inert>
 <div class="profile-wizard-step-heading">
 <h3>Cuenta</h3>
 </div>
 <div class="profile-account-stack">
 <section class="profile-content-card">
 <div class="profile-content-card-heading">
 <span class="profile-feature-icon" aria-hidden="true">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M10 21h4"/></svg>
 </span>
 <div>
 <h4>Notificaciones</h4>
 </div>
 </div>
 <BotonNotificaciones client:idle userId={userId} />
 </section>
 <section class="profile-content-card">
 <div class="profile-content-card-heading">
 <span class="profile-feature-icon profile-feature-icon--security" aria-hidden="true">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9"><path d="M12 3 4 7v5c0 5 3.4 8.3 8 9 4.6-.7 8-4 8-9V7l-8-4Z"/><path d="M9 12l2 2 4-4"/></svg>
 </span>
 <div>
 <h4>Seguridad</h4>
 </div>
 </div>
 <div class="profile-account-actions">
 <button id="btn-change-password" type="button">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"/></svg>
 Cambiar contrase&ntilde;a
 </button>
 <button id="btn-logout" type="button">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" aria-hidden="true"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><path d="m16 17 5-5-5-5M21 12H9"/></svg>
 Cerrar sesi&oacute;n
 </button>
 </div>
 </section>
 </div>
 </section>
 </div>

 <div class="profile-save-dock" aria-live="polite">
 <button type="submit" id="btn-save-datos" form="form-datos-personales" class="profile-wizard-save hidden">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true"><path d="m5 13 4 4L19 7"/></svg>
 Guardar cambios
 </button>
 </div>

 <div id="profile-feedback" class="profile-feedback" data-tone="success" role="status" aria-live="polite" aria-atomic="true" hidden>
 <span class="profile-feedback-icon" aria-hidden="true">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
 <path class="profile-feedback-success-icon" d="m5 13 4 4L19 7"/>
 <path class="profile-feedback-error-icon" d="m7 7 10 10M17 7 7 17"/>
 </svg>
 </span>
 <p id="profile-feedback-message"></p>
 <button id="profile-feedback-close" type="button" aria-label="Cerrar notificaci&oacute;n">
 <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 6 12 12M18 6 6 18"/></svg>
 </button>
 </div>
 </article>
 </main>

 <OnboardingRoot client:idle page="perfil" userId={userId} />

 {/* Modal para Recortar Foto */}
 <div id="cropper-modal" class="fixed inset-0 z-50 bg-black/80 hidden items-center justify-center p-4 backdrop-blur-sm transition-opacity opacity-0 duration-300">
 <div id="cropper-content" class="bg-surface rounded-2xl w-full max-w-lg overflow-hidden shadow-2xl scale-95 transition-transform duration-300">
 <div class="px-6 py-4 border-b border-border flex justify-between items-center">
 <h3 class="text-lg font-bold text-content ">Ajustar Foto de Perfil</h3>
 <button id="btn-close-cropper" class="text-content-muted hover:text-content-muted dark:text-neutral-200 transition-colors">
 <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
 </button>
 </div>
 
 <div class="px-6 py-4 bg-blue-50 text-blue-900 border-b border-blue-200 dark:bg-blue-900/40 dark:text-blue-100 dark:border-blue-800 flex items-start gap-3">
 <div class="mt-0.5 text-blue-700 dark:text-blue-200 shrink-0">
 <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
 </div>
 <div class="space-y-1">
 <p class="text-sm font-bold leading-snug">Usa una foto de cerca (Retrato)</p>
 <p class="text-sm leading-snug text-blue-900 dark:text-blue-100">Ampl&iacute;a la imagen hasta que <strong class="text-blue-700 dark:text-white">tu rostro llene el c&iacute;rculo</strong>. No uses fotos de cuerpo entero ni grupales.</p>
 </div>
 </div>

 <div class="p-6 bg-background aspect-square flex items-center justify-center relative">
 <div class="w-full h-full max-h-[60vh] relative z-10">
 <img id="cropper-image" src="" alt="Imagen a recortar" class="max-w-full hidden" />
 </div>
 </div>
 <div class="px-6 py-4 border-t border-border flex justify-end gap-3 bg-background ">
 <button id="btn-cancel-cropper" class="px-5 py-2.5 rounded-xl font-bold text-sm text-content-muted hover:bg-neutral/20 dark:bg-neutral-800 transition-colors">Cancelar</button>
 <button id="btn-save-cropper" class="px-5 py-2.5 rounded-xl font-ui-strong text-sm bg-action hover:bg-action/80 text-white shadow-md transition-all flex items-center gap-2">
 <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>
 Guardar Foto
 </button>
 </div>
 </div>
 </div>
</Layout>

<div id="avatar-lock-modal" class="fixed inset-0 z-[55] hidden items-center justify-center bg-black/60 p-4 backdrop-blur-sm opacity-0 transition-opacity duration-300">
 <div id="avatar-lock-content" class="w-full max-w-md rounded-[28px] border border-sky-200/80 bg-white shadow-2xl shadow-sky-950/10 transition-transform duration-300 scale-95 dark:border-sky-800/50 dark:bg-surface dark:shadow-black/40">
 <div class="flex items-center justify-between border-b border-border px-6 py-5">
 <div class="flex items-center gap-3">
 <div class="rounded-2xl bg-sky-100 p-2.5 text-sky-700 dark:bg-sky-900/60 dark:text-sky-200">
 <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
 <path d="M12 3a9 9 0 1 0 9 9"></path>
 <path d="M12 7v5l3 3"></path>
 </svg>
 </div>
 <div>
 <h3 class="text-lg font-bold text-content">Cambio de foto de perfil</h3>
 <p class="text-sm text-content-muted">Informacion importante</p>
 </div>
 </div>
 <button id="btn-close-avatar-lock" class="inline-flex h-11 w-11 items-center justify-center rounded-2xl border border-border bg-surface text-content-muted transition-colors hover:text-content hover:bg-background" type="button" aria-label="Cerrar aviso">
 <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
 </button>
 </div>

 <div class="space-y-4 px-6 py-6">
 <p class="rounded-3xl border border-sky-200/80 bg-gradient-to-br from-sky-50 via-white to-amber-50 px-5 py-5 text-sm leading-7 text-slate-700 shadow-sm shadow-sky-100/70 dark:border-sky-800/50 dark:from-sky-950/40 dark:via-surface dark:to-amber-950/10 dark:text-slate-200 dark:shadow-none">
 <strong class="font-bold text-slate-900 dark:text-slate-50">&iexcl;Hola, Dios te bendiga!</strong>
 Para poder <strong class="font-semibold text-slate-900 dark:text-slate-50">identificarte f&aacute;cilmente en el calendario y en las asignaciones</strong>, hemos actualizado tu foto de perfil con una donde
 <strong class="font-semibold text-slate-900 dark:text-slate-50">se ve tu rostro claramente</strong>.
 Si prefieres usar una distinta, <strong class="font-semibold text-slate-900 dark:text-slate-50">&iexcl;no hay problema!</strong>
 Solo env&iacute;ale
 <strong class="font-semibold text-slate-900 dark:text-slate-50">una foto clara de tu rostro al administrador</strong>
 y con gusto la cambiaremos.
 <strong class="font-semibold text-slate-900 dark:text-slate-50">&iexcl;Un abrazo y gracias por tu comprensi&oacute;n!</strong>
 </p>
 </div>

 <div class="flex justify-end border-t border-border px-6 py-5">
 <button id="btn-confirm-avatar-lock" type="button" class="inline-flex min-h-[48px] items-center justify-center rounded-2xl bg-action px-5 py-3 text-sm font-bold text-white shadow-sm transition-colors hover:bg-action/85">
 Entendido
 </button>
 </div>
 </div>
</div>

<style is:global>
 .profile-wizard-shell {
  display: flex;
  width: 100%;
  min-height: 640px;
  height: calc(100dvh - 72px);
  align-items: stretch;
  justify-content: center;
  padding: 0;
 }

 .profile-wizard-panel {
  position: relative;
  display: flex;
  width: 100%;
  height: 100%;
  min-height: 0;
  flex-direction: column;
  overflow: hidden;
  color: rgb(var(--text-base));
  border: 0;
  border-radius: 0;
  background: rgb(var(--bg-background));
  box-shadow: none;
 }

 .profile-wizard-hero {
  position: relative;
  height: 280px;
  flex: 0 0 280px;
  overflow: hidden;
  isolation: isolate;
  color: white;
  background: linear-gradient(135deg, rgb(15 23 42), rgb(30 64 175) 62%, rgb(14 116 144));
 }

 .profile-wizard-hero::after {
  position: absolute;
  inset: 0;
  z-index: 1;
  content: '';
  pointer-events: none;
  background:
   radial-gradient(circle at 78% 6%, rgb(255 255 255 / 0.28), transparent 28%),
   linear-gradient(180deg, rgb(255 255 255 / 0.08), transparent 42%, rgb(2 6 23 / 0.12));
 }

 .profile-wizard-hero-backdrop {
  position: absolute;
  inset: -30px;
  z-index: 0;
  overflow: hidden;
  pointer-events: none;
  opacity: 0;
  transform: scale(1.06);
  transition: opacity 0.55s ease;
 }

 .profile-wizard-hero-backdrop.has-image {
  opacity: 1;
 }

 .profile-wizard-hero-backdrop img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 34%;
  opacity: 0.74;
  filter: blur(22px) saturate(0.8) contrast(0.96);
  transform: scale(1.1);
 }

 .profile-wizard-hero-backdrop::after {
  position: absolute;
  inset: 0;
  content: '';
  background:
   linear-gradient(105deg, rgb(15 23 42 / 0.62), rgb(30 64 175 / 0.5) 58%, rgb(8 47 73 / 0.66)),
   linear-gradient(0deg, rgb(2 6 23 / 0.62), transparent 64%),
   radial-gradient(circle at 82% 12%, rgb(96 165 250 / 0.26), transparent 34%);
  box-shadow: inset 0 1px 0 rgb(255 255 255 / 0.18);
  backdrop-filter: saturate(1.04);
 }

 .profile-wizard-hero-glow {
  position: absolute;
  top: -80px;
  right: 8%;
  z-index: 1;
  width: 280px;
  height: 280px;
  border-radius: 999px;
  background: rgb(255 255 255 / 0.13);
  filter: blur(30px);
 }

 .profile-wizard-portrait-wrap {
  position: absolute;
  z-index: 2;
  bottom: 30px;
  left: max(28px, 3vw);
  width: 150px;
  height: 170px;
 }

 .profile-wizard-portrait {
  display: flex;
  width: 100%;
  height: 100%;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  color: white;
  border: 2px solid rgb(255 255 255 / 0.72);
  border-radius: 1.8rem;
  background: rgb(255 255 255 / 0.12);
  box-shadow: 0 24px 48px -25px rgb(2 6 23 / 0.94), inset 0 1px 0 rgb(255 255 255 / 0.2);
  font-size: 3.2rem;
  font-weight: 950;
  letter-spacing: -0.06em;
  backdrop-filter: blur(18px);
 }

 .profile-wizard-portrait img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 34%;
 }

 .profile-wizard-portrait #avatar-preview-text {
  color: white;
 }

 .profile-wizard-photo-button {
  position: absolute;
  z-index: 3;
  top: auto;
  right: auto;
  bottom: 39px;
  left: calc(max(28px, 3vw) + 100px);
  display: inline-flex;
  width: 44px;
  height: 44px;
  min-height: 42px;
  align-items: center;
  justify-content: center;
  padding: 0;
  color: white;
  border: 1px solid rgb(255 255 255 / 0.52);
  border-radius: 50%;
  background: rgb(15 23 42 / 0.68);
  box-shadow: 0 12px 24px -13px rgb(2 6 23 / 0.95), inset 0 1px 0 rgb(255 255 255 / 0.18);
  backdrop-filter: blur(16px);
  transition: transform 0.2s ease, background-color 0.2s ease, opacity 0.2s ease;
 }

 .profile-wizard-photo-button span {
  display: none;
 }

 .profile-wizard-photo-button svg {
  width: 16px;
  height: 16px;
 }

 .profile-wizard-photo-button:hover:not(:disabled) {
  transform: translateY(-1px) scale(1.04);
  background: rgb(15 23 42 / 0.86);
 }

 .profile-wizard-photo-button:disabled {
  cursor: wait;
  opacity: 0.56;
 }

 .profile-wizard-photo-button:focus-visible {
  outline: 3px solid rgb(255 255 255 / 0.72);
  outline-offset: 2px;
 }

 .profile-wizard-identity {
  position: absolute;
  z-index: 2;
  right: max(80px, 6vw);
  bottom: 30px;
  left: max(230px, 18vw);
  width: auto;
  min-width: 0;
  padding: 0;
  border: 0;
  background: transparent;
  box-shadow: none;
  backdrop-filter: none;
 }

 .profile-wizard-eyebrow {
  margin-bottom: 0.35rem;
  color: rgb(255 255 255 / 0.6);
  font-size: 0.65rem;
  font-weight: 950;
  letter-spacing: 0.16em;
  text-transform: uppercase;
 }

 .profile-wizard-identity h2 {
  width: fit-content;
  max-width: 100%;
  overflow: hidden;
  color: white;
  font-size: clamp(1.9rem, 3.6vw, 2.9rem);
  font-weight: 950;
  line-height: 1.02;
  letter-spacing: -0.045em;
  text-overflow: ellipsis;
  text-shadow: 0 3px 20px rgb(2 6 23 / 0.36);
  white-space: nowrap;
 }

 .profile-wizard-identity > p:not(.profile-wizard-eyebrow) {
  width: fit-content;
  max-width: 100%;
  margin-top: 0.38rem;
  overflow: hidden;
  color: rgb(255 255 255 / 0.68);
  font-size: 0.78rem;
  font-weight: 650;
  text-overflow: ellipsis;
  white-space: nowrap;
 }

 .profile-wizard-roles {
  display: flex;
  width: fit-content;
  max-width: 100%;
  min-height: 44px;
  align-items: center;
  flex-wrap: nowrap;
  gap: 0;
  margin-top: 0.8rem;
  padding: 0.5rem 0.72rem;
  overflow-x: auto;
  border: 1px solid rgb(255 255 255 / 0.22);
  border-radius: 1rem;
  background: rgb(15 23 42 / 0.32);
  box-shadow: inset 0 1px 0 rgb(255 255 255 / 0.12), 0 14px 30px -24px rgb(2 6 23 / 0.9);
  scrollbar-width: none;
  backdrop-filter: blur(16px) saturate(1.12);
 }

 .profile-wizard-roles::-webkit-scrollbar {
  display: none;
 }

 .profile-wizard-roles > span {
  position: relative;
  flex: 0 0 auto;
  padding: 0.18rem 0.62rem !important;
  color: rgb(255 255 255 / 0.88) !important;
  border: 0 !important;
  border-radius: 0 !important;
  background: transparent !important;
  box-shadow: none !important;
  font-size: 0.58rem !important;
  letter-spacing: 0.055em !important;
  white-space: nowrap;
 }

 .profile-wizard-roles > span + span::before {
  position: absolute;
  top: 20%;
  bottom: 20%;
  left: 0;
  width: 1px;
  content: '';
  background: rgb(255 255 255 / 0.22);
 }

 .profile-wizard-nav {
  position: relative;
  z-index: 3;
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  flex: 0 0 auto;
  gap: 0.3rem;
  margin: 0;
  padding: 0.68rem max(1rem, calc((100vw - 980px) / 2));
  border: 0;
  border-bottom: 1px solid rgb(var(--border-base));
  border-radius: 0;
  background: rgb(var(--bg-surface) / 0.96);
  box-shadow: 0 12px 32px -28px rgb(15 23 42 / 0.68);
  backdrop-filter: blur(20px) saturate(1.1);
 }

 .profile-wizard-nav-item {
  display: flex;
  min-width: 0;
  min-height: 48px;
  align-items: center;
  justify-content: center;
  gap: 0.45rem;
  padding: 0.45rem 0.65rem;
  color: rgb(var(--text-muted));
  border: 1px solid transparent;
  border-radius: 1.05rem;
  font-size: 0.72rem;
  font-weight: 850;
  transition: color 0.22s ease, background-color 0.22s ease, border-color 0.22s ease, transform 0.22s ease;
 }

 .profile-wizard-nav-item svg {
  width: 17px;
  height: 17px;
  flex: 0 0 auto;
 }

 .profile-wizard-nav-item[aria-selected='true'] {
  color: white;
  border-color: rgb(255 255 255 / 0.28);
  background: linear-gradient(110deg, rgb(59 130 246), rgb(129 140 248));
  box-shadow: 0 12px 24px -16px rgb(37 99 235 / 0.9), inset 0 1px 0 rgb(255 255 255 / 0.28);
 }

 .profile-wizard-nav-item:not([aria-selected='true']):hover {
  color: rgb(var(--text-base));
  transform: translateY(-1px);
 }

 .profile-wizard-nav-item[aria-selected='true']:hover {
  color: white;
 }

 .profile-wizard-nav-item:focus-visible {
  outline: 3px solid rgb(var(--color-brand) / 0.28);
  outline-offset: 1px;
 }

 .profile-wizard-stage {
  position: relative;
  min-height: 0;
  flex: 1 1 auto;
  overflow: hidden;
  margin-top: 0;
  padding-top: 0;
  background:
   radial-gradient(circle at 100% 0%, rgb(var(--color-brand) / 0.1), transparent 32%),
   linear-gradient(180deg, color-mix(in srgb, rgb(var(--bg-background)) 92%, rgb(var(--color-brand)) 8%), rgb(var(--bg-surface)) 38%);
 }

 .profile-wizard-step {
  position: absolute;
  inset: 0;
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  padding: 2.45rem max(1.25rem, calc((100vw - 980px) / 2)) 5.4rem;
  opacity: 0;
  pointer-events: none;
  transform: translateX(36px) scale(0.992);
  transition: opacity 0.3s ease, transform 0.46s cubic-bezier(0.22, 1, 0.36, 1);
 }

 .profile-wizard-step[data-state='before'] {
  transform: translateX(-36px) scale(0.992);
 }

 .profile-wizard-step[data-state='active'] {
  opacity: 1;
  pointer-events: auto;
  transform: translateX(0) scale(1);
 }

 .profile-wizard-step-heading {
  margin-bottom: 1.4rem;
 }

 .profile-wizard-step-heading h3 {
  color: rgb(var(--text-base));
  font-size: 1.35rem;
  font-weight: 950;
  line-height: 1.08;
  letter-spacing: -0.036em;
 }

 .profile-form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0;
  overflow: hidden;
  border: 1px solid rgb(var(--border-base));
  border-radius: 1.4rem;
  background: rgb(var(--bg-surface));
  box-shadow: 0 14px 32px -28px rgb(15 23 42 / 0.5);
 }

 .profile-form-grid .profile-field {
  padding: 1rem 1.1rem;
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
 }

 .profile-form-grid .profile-field:nth-child(odd) {
  border-right: 1px solid rgb(var(--border-base));
 }

 .profile-form-grid .profile-field:nth-child(-n + 2) {
  border-bottom: 1px solid rgb(var(--border-base));
 }

 .profile-form-grid .profile-field input {
  min-height: 34px;
  padding: 0.25rem 0;
  border: 0;
  border-radius: 0.45rem;
  background: transparent;
 }

 .profile-form-grid .profile-field input:focus-visible {
  padding-inline: 0.55rem;
  background: rgb(var(--bg-background));
 }

 .profile-field {
  display: block;
  min-width: 0;
  padding: 0.9rem;
  border: 1px solid rgb(var(--border-base));
  border-radius: 1.15rem;
  background: rgb(var(--bg-surface));
  box-shadow: 0 12px 26px -25px rgb(15 23 42 / 0.54);
 }

 .profile-field > span {
  display: block;
  margin-bottom: 0.45rem;
  color: rgb(var(--text-muted));
  font-size: 0.62rem;
  font-weight: 900;
  letter-spacing: 0.085em;
  text-transform: uppercase;
 }

 .profile-field input,
 .profile-field select {
  width: 100%;
  min-width: 0;
  min-height: 46px;
  padding: 0.65rem 0.8rem;
  color: rgb(var(--text-base));
  border: 1px solid rgb(var(--border-base));
  border-radius: 0.85rem;
  background: rgb(var(--bg-background));
  font-size: 0.78rem;
  font-weight: 700;
  outline: none;
  transition: border-color 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
 }

 .profile-field input:focus-visible,
 .profile-field select:focus-visible {
  border-color: rgb(var(--color-brand));
  box-shadow: 0 0 0 3px rgb(var(--color-brand) / 0.2);
 }

 .profile-field input:disabled {
  cursor: not-allowed;
  opacity: 0.66;
 }

 .profile-voice-card {
  display: block;
  padding: 0;
  border: 0;
  border-radius: 0;
  background: transparent;
 }

 .profile-feature-icon {
  display: inline-flex;
  width: 46px;
  height: 46px;
  flex: 0 0 auto;
  align-items: center;
  justify-content: center;
  color: rgb(var(--color-action));
  border: 1px solid rgb(var(--color-action) / 0.2);
  border-radius: 1rem;
  background: rgb(var(--color-action) / 0.08);
 }

 .profile-feature-icon svg {
  width: 20px;
  height: 20px;
 }

 .profile-feature-icon--agenda {
  color: rgb(217 119 6);
  border-color: rgb(245 158 11 / 0.24);
  background: rgb(245 158 11 / 0.08);
 }

 .profile-feature-icon--security {
  color: rgb(5 150 105);
  border-color: rgb(16 185 129 / 0.24);
  background: rgb(16 185 129 / 0.08);
 }

 .profile-voice-field {
  margin-top: 0;
  padding: 0;
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
 }

 .profile-agenda-stack,
 .profile-account-stack {
  display: grid;
  gap: 0.85rem;
 }

 .profile-calendar-slot > * {
  margin: 0 !important;
 }

 .profile-content-card {
  padding: 1rem;
  border: 1px solid rgb(var(--border-base));
  border-radius: 1.45rem;
  background: rgb(var(--bg-surface));
  box-shadow: 0 12px 28px -26px rgb(15 23 42 / 0.55);
 }

 .profile-agenda-stack .profile-content-card {
  padding: 0;
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
 }

 .profile-absence-section {
  margin-top: 1.15rem;
  padding-top: 1.55rem !important;
  border-top: 1px solid rgb(var(--border-base)) !important;
 }

 .profile-content-card-heading {
  display: flex;
  align-items: center;
  gap: 0.8rem;
  margin-bottom: 0.9rem;
 }

 .profile-content-card-heading h4 {
  color: rgb(var(--text-base));
  font-size: 0.9rem;
  font-weight: 900;
 }

 .profile-content-card-heading p {
  margin-top: 0.2rem;
  color: rgb(var(--text-muted));
  font-size: 0.66rem;
  font-weight: 580;
  line-height: 1.4;
 }

 .profile-absence-form {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0;
  overflow: hidden;
  border: 1px solid rgb(var(--border-base));
  border-radius: 1.3rem;
  background: rgb(var(--bg-surface));
 }

 .profile-absence-reason {
  grid-column: 1 / -1;
 }

 .profile-absence-form .profile-field {
  border: 0;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
 }

 .profile-absence-form .profile-field:first-child {
  border-right: 1px solid rgb(var(--border-base));
 }

 .profile-absence-form .profile-absence-reason {
  border-top: 1px solid rgb(var(--border-base));
 }

 .profile-absence-form .profile-field input {
  border-color: transparent;
  background: transparent;
 }

 .profile-absence-reason em {
  font-style: normal;
  font-weight: 650;
  letter-spacing: 0;
  text-transform: none;
 }

 .profile-absence-action-row {
  display: flex;
  gap: 0.55rem;
 }

 .profile-absence-action-row button {
  min-height: 46px;
  flex: 0 0 auto;
  padding: 0.65rem 1rem;
  color: white;
  border-radius: 0.85rem;
  background: rgb(234 88 12);
  font-size: 0.74rem;
  font-weight: 900;
  transition: transform 0.2s ease, background-color 0.2s ease;
 }

 .profile-absence-action-row button:hover {
  transform: translateY(-1px);
  background: rgb(194 65 12);
 }

 .profile-content-note {
  margin: 0.75rem 0;
  color: rgb(var(--text-muted));
  font-size: 0.64rem;
  font-weight: 570;
  line-height: 1.45;
 }

 .profile-empty-state {
  padding: 1.25rem;
  color: rgb(var(--text-muted));
  border: 1px dashed rgb(var(--border-base));
  border-radius: 1rem;
  font-size: 0.68rem;
  font-weight: 750;
  text-align: center;
 }

 .profile-account-actions {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.65rem;
 }

 .profile-account-actions button {
  display: inline-flex;
  min-height: 50px;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.7rem 0.9rem;
  color: rgb(var(--text-base));
  border: 1px solid rgb(var(--border-base));
  border-radius: 1rem;
  background: rgb(var(--bg-background));
  font-size: 0.74rem;
  font-weight: 850;
  transition: transform 0.2s ease, border-color 0.2s ease, background-color 0.2s ease;
 }

 .profile-account-actions button svg {
  width: 17px;
  height: 17px;
 }

 .profile-account-actions button:hover {
  transform: translateY(-1px);
  border-color: rgb(var(--color-brand) / 0.32);
 }

 .profile-account-actions #btn-logout {
  color: rgb(220 38 38);
  border-color: rgb(239 68 68 / 0.17);
  background: rgb(239 68 68 / 0.07);
 }

 .profile-save-dock {
  position: absolute;
  right: max(1.25rem, calc((100vw - 980px) / 2));
  bottom: 1.15rem;
  z-index: 5;
  pointer-events: none;
 }

 .profile-wizard-save {
  display: inline-flex;
  min-width: 176px;
  min-height: 50px;
  align-items: center;
  justify-content: center;
  gap: 0.4rem;
  padding: 0.72rem 1.05rem;
  color: white;
  border: 1px solid rgb(255 255 255 / 0.3);
  border-radius: 999px;
  background: linear-gradient(110deg, rgb(37 99 235), rgb(129 140 248));
  box-shadow: 0 18px 34px -20px rgb(37 99 235 / 0.92), inset 0 1px 0 rgb(255 255 255 / 0.3);
  font-size: 0.76rem;
  font-weight: 900;
  pointer-events: auto;
  backdrop-filter: blur(18px);
  transition: transform 0.2s ease, filter 0.22s ease, opacity 0.22s ease;
 }

 .profile-wizard-save.hidden {
  display: none;
 }

 .profile-wizard-save svg {
  width: 16px;
  height: 16px;
 }

 .profile-wizard-save:hover {
  transform: translateY(-1px);
  filter: brightness(1.06);
 }

 .profile-wizard-save:disabled {
  cursor: wait;
  opacity: 0.58;
  transform: none;
 }

 .profile-wizard-save:focus-visible,
 .profile-account-actions button:focus-visible,
 .profile-absence-action-row button:focus-visible,
 .profile-feedback button:focus-visible {
  outline: 3px solid rgb(var(--color-brand) / 0.3);
  outline-offset: 2px;
 }

 .profile-feedback {
  position: absolute;
  bottom: 1.15rem;
  left: 50%;
  z-index: 20;
  display: grid;
  width: min(390px, calc(100% - 2rem));
  min-height: 54px;
  grid-template-columns: auto minmax(0, 1fr) auto;
  align-items: center;
  gap: 0.7rem;
  padding: 0.7rem 0.72rem 0.7rem 0.78rem;
  color: rgb(var(--text-base));
  border: 1px solid rgb(var(--border-base));
  border-radius: 1.05rem;
  background: rgb(var(--bg-surface) / 0.96);
  box-shadow: 0 20px 46px -24px rgb(15 23 42 / 0.72), inset 0 1px 0 rgb(255 255 255 / 0.36);
  opacity: 0;
  pointer-events: none;
  transform: translate(-50%, 14px) scale(0.98);
  backdrop-filter: blur(18px);
  transition: opacity 0.2s ease, transform 0.28s cubic-bezier(0.22, 1, 0.36, 1);
 }

 .profile-feedback[hidden] {
  display: none;
 }

 .profile-feedback[data-visible='true'] {
  opacity: 1;
  pointer-events: auto;
  transform: translate(-50%, 0) scale(1);
 }

 .profile-feedback-icon {
  display: inline-flex;
  width: 34px;
  height: 34px;
  align-items: center;
  justify-content: center;
  color: rgb(5 150 105);
  border-radius: 0.8rem;
  background: rgb(16 185 129 / 0.12);
 }

 .profile-feedback[data-tone='error'] .profile-feedback-icon {
  color: rgb(220 38 38);
  background: rgb(239 68 68 / 0.11);
 }

 .profile-feedback-error-icon,
 .profile-feedback[data-tone='error'] .profile-feedback-success-icon {
  display: none;
 }

 .profile-feedback[data-tone='error'] .profile-feedback-error-icon {
  display: block;
 }

 .profile-feedback[data-tone='info'] .profile-feedback-icon {
  color: rgb(var(--color-action));
  background: rgb(var(--color-action) / 0.1);
 }

 .profile-feedback-icon svg,
 .profile-feedback button svg {
  width: 17px;
  height: 17px;
 }

 .profile-feedback p {
  font-size: 0.72rem;
  font-weight: 760;
  line-height: 1.42;
 }

 .profile-feedback button {
  display: inline-flex;
  width: 36px;
  height: 36px;
  align-items: center;
  justify-content: center;
  color: rgb(var(--text-muted));
  border-radius: 0.75rem;
  transition: color 0.18s ease, background-color 0.18s ease;
 }

 .profile-feedback button:hover {
  color: rgb(var(--text-base));
  background: rgb(var(--bg-background));
 }

 html.dark .profile-feature-icon--agenda {
  color: rgb(253 186 116);
 }

 html.dark .profile-feature-icon--security {
  color: rgb(110 231 183);
 }

 html.dark .profile-wizard-nav {
  border-color: rgb(255 255 255 / 0.12);
  background: rgb(var(--bg-surface) / 0.76);
  box-shadow: 0 18px 40px -27px rgb(0 0 0 / 0.86), inset 0 1px 0 rgb(255 255 255 / 0.08);
 }

 @media (max-width: 639px) {
  .profile-wizard-shell {
   display: block;
   width: 100vw;
   max-width: 100vw;
   height: auto;
   min-height: calc(100dvh - 66px);
   margin-right: calc(50% - 50vw);
   margin-left: calc(50% - 50vw);
   padding: 0;
  }

  .profile-wizard-panel {
   width: 100%;
   height: auto;
   min-height: calc(100dvh - 66px);
   overflow: visible;
   border-width: 0;
   border-radius: 0;
   box-shadow: none;
  }

  .profile-wizard-hero {
   position: sticky;
   top: 0;
   z-index: 0;
   height: min(420px, 48dvh);
   flex-basis: min(420px, 48dvh);
  }

  .profile-wizard-hero-backdrop {
   inset: 0;
   transform: none;
   will-change: transform;
  }

  .profile-wizard-hero-backdrop img {
   top: -24px;
   width: 100%;
   height: calc(100% + 48px);
   object-position: center 32%;
   opacity: 1;
   filter: none;
   transform: translate3d(0, var(--profile-parallax-y, 0px), 0) scale(1.06);
   transform-origin: center center;
   will-change: transform;
  }

  .profile-wizard-hero-backdrop::before {
   position: absolute;
   inset: 0;
   z-index: 1;
   content: '';
   pointer-events: none;
   -webkit-mask-image: radial-gradient(ellipse at center, transparent 56%, rgb(0 0 0 / 0.46) 76%, black 100%);
   mask-image: radial-gradient(ellipse at center, transparent 56%, rgb(0 0 0 / 0.46) 76%, black 100%);
   -webkit-backdrop-filter: blur(6px);
   backdrop-filter: blur(6px);
  }

  .profile-wizard-hero-backdrop::after {
   z-index: 2;
   background:
    linear-gradient(0deg, rgb(2 6 23 / 0.92) 0%, rgb(2 6 23 / 0.46) 34%, transparent 72%),
    linear-gradient(180deg, rgb(15 23 42 / 0.16), transparent 34%);
  }

  .profile-wizard-photo-button {
   top: auto;
   right: 20px;
   bottom: 102px;
   left: auto;
   width: 44px;
   min-height: 44px;
   justify-content: center;
   padding: 0;
  }

  .profile-wizard-portrait-wrap {
   display: none;
  }

  .profile-wizard-photo-button span {
   display: none;
  }

  .profile-wizard-identity {
   right: 20px;
   bottom: 30px;
   left: 20px;
   width: auto;
   padding: 0;
  }

  .profile-wizard-identity h2 {
   max-width: calc(100% - 58px);
   font-size: clamp(1.8rem, 8vw, 2.35rem);
  }

  .profile-wizard-identity > p:not(.profile-wizard-eyebrow) {
   font-size: 0.72rem;
  }

  .profile-wizard-roles {
   width: 100%;
   max-height: none;
   margin-top: 0.7rem;
   padding: 0.48rem 0.35rem;
  }

  .profile-wizard-roles > span {
   padding: 0.18rem 0.48rem !important;
   font-size: 0.5rem !important;
  }

  .profile-wizard-nav {
   position: sticky;
   top: 0;
   z-index: 15;
   gap: 0.12rem;
   margin: -24px 0 0;
   padding: 0.9rem 0.45rem 0.48rem;
   border-top: 1px solid rgb(255 255 255 / 0.76);
   border-right: 0;
   border-bottom: 1px solid rgb(var(--border-base));
   border-left: 0;
   border-radius: 1.8rem 1.8rem 0 0;
   background: rgb(var(--bg-surface) / 0.97);
   box-shadow: 0 -16px 36px -30px rgb(15 23 42 / 0.7);
  }

  .profile-wizard-nav-item {
   min-height: 52px;
   flex-direction: column;
   gap: 0.16rem;
   padding: 0.3rem 0.12rem;
   font-size: 0.58rem;
  }

  .profile-wizard-nav-item svg {
   width: 15px;
   height: 15px;
  }

  .profile-wizard-step {
   position: relative;
   inset: auto;
   display: none;
   overflow: visible;
   padding: 2rem 0.85rem 5.7rem;
  }

  .profile-wizard-stage {
   z-index: 8;
   min-height: 100dvh;
   flex: none;
   overflow: visible;
   background:
    radial-gradient(circle at 92% -8%, rgb(var(--color-brand) / 0.14), transparent 32%),
    linear-gradient(
     180deg,
     rgb(var(--bg-background) / 0.82) 0%,
     rgb(var(--bg-background) / 0.94) 12rem,
     rgb(var(--bg-background) / 0.985) 34rem
    );
   box-shadow:
    inset 0 1px 0 rgb(255 255 255 / 0.08),
    0 -24px 56px -38px rgb(0 0 0 / 0.92);
   -webkit-backdrop-filter: blur(24px) saturate(1.28);
   backdrop-filter: blur(24px) saturate(1.28);
  }

  .profile-wizard-step[data-state='active'] {
   display: block;
   animation: profile-mobile-panel-enter 0.38s cubic-bezier(0.22, 1, 0.36, 1);
  }

  @keyframes profile-mobile-panel-enter {
   from {
    opacity: 0.25;
    transform: translateY(12px);
   }
   to {
    opacity: 1;
    transform: translateY(0);
   }
  }

  .profile-form-grid,
  .profile-absence-form,
  .profile-account-actions {
   grid-template-columns: 1fr;
  }

  .profile-form-grid .profile-field:nth-child(odd) {
   border-right: 0;
  }

  .profile-form-grid .profile-field:not(:last-child) {
   border-bottom: 1px solid rgb(var(--border-base));
  }

  .profile-absence-form .profile-field:first-child {
   border-right: 0;
  }

  .profile-absence-form .profile-field:not(:last-child) {
   border-bottom: 1px solid rgb(var(--border-base));
  }

  .profile-absence-form .profile-absence-reason {
   border-top: 0;
  }

  .profile-absence-reason {
   grid-column: auto;
  }

  .profile-save-dock {
   position: fixed;
   right: 0.8rem;
   bottom: calc(76px + env(safe-area-inset-bottom));
   left: 0.8rem;
  }

  .profile-absence-section {
   margin-top: 1rem;
   padding-top: 1.3rem !important;
  }

  .profile-feedback {
   position: fixed;
   right: 0.8rem;
   bottom: calc(82px + env(safe-area-inset-bottom));
   left: 0.8rem;
   width: auto;
   transform: translateY(14px) scale(0.98);
  }

  .profile-feedback[data-visible='true'] {
   transform: translateY(0) scale(1);
  }

  .profile-wizard-save {
   width: 100%;
   min-width: 0;
   font-size: 0.72rem;
  }
 }

 @media (max-width: 380px) {
  .profile-absence-action-row {
   flex-direction: column;
  }
 }

 @media (prefers-reduced-motion: reduce) {
  .profile-wizard-step,
  .profile-wizard-hero-backdrop,
  .profile-wizard-photo-button,
  .profile-wizard-nav-item {
   transition-duration: 0.01ms !important;
  }
 }

 .cropper-view-box,
 .cropper-face {
 border-radius: 50%;
 }
</style>

<!-- Script de Cropperjs vÃ­a CDN -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.css" integrity="sha512-hvNR0F/e2J7zPPfLC9auFe3/SE0yG4aJCOd/qxew74NN7eyiSKjr7xJJMu1Jy2wf7FXITpWS1E/RY8yzuXN7VA==" crossorigin="anonymous" />
<script is:inline src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.js" integrity="sha512-9KkIqdfN7ipEW6B6k+Aq20PV31bjODg4AA52W+tYtAE0jE0kMx49bjJ3FgvS56wzmyfMUHbQ4Km2b7l9+Y/+Eg==" crossorigin="anonymous"></script>

<script>
 import { supabase, uploadAvatarAtomic } from '../lib/supabase';
 import { getSafeImageForCropper } from '../utils/safeImageProcessor';
 import { safeHtml, toSafeMediaUrl } from '../lib/client/html-safety.js';

 const revokePerfilSafeCropperUrl = () => {
 const safeUrl = (window as any).__perfilSafeCropperUrl as string | null | undefined;
 if (!safeUrl) return;
 URL.revokeObjectURL(safeUrl);
 (window as any).__perfilSafeCropperUrl = null;
 };

 const cleanupPerfilRuntime = () => {
 const abortController = (window as any).__perfilListenersAbort as AbortController | undefined;
 if (abortController) {
 abortController.abort();
 (window as any).__perfilListenersAbort = null;
 }

 const cropperInstance = (window as any)._cropperInstance;
 if (cropperInstance) {
 try {
 cropperInstance.destroy();
 } catch {
 // no-op
 }
 (window as any)._cropperInstance = null;
 }

 const parallaxFrame = Number((window as any).__perfilParallaxFrame || 0);
 if (parallaxFrame) cancelAnimationFrame(parallaxFrame);
 (window as any).__perfilParallaxFrame = 0;
 revokePerfilSafeCropperUrl();
 };

 const initPerfilPage = async () => {
 cleanupPerfilRuntime();
 const listenersAbort = new AbortController();
 const signal = listenersAbort.signal;
 (window as any).__perfilListenersAbort = listenersAbort;

 const on = (
 target: EventTarget | null | undefined,
 eventName: string,
 handler: EventListenerOrEventListenerObject,
 options: AddEventListenerOptions = {},
 ) => {
 if (!target || typeof (target as EventTarget).addEventListener !== 'function') return;
 (target as EventTarget).addEventListener(eventName, handler, { ...options, signal });
 };

 // Init global vars
 let currentUserId: string | null = null;
 let currentAvatarUrl: string | null = null;

 const wait = (ms: number) => new Promise((resolve) => setTimeout(resolve, ms));
 const getCookieValue = (name: string) => {
 try {
 const cookiePart = document.cookie
 .split(';')
 .map((chunk) => chunk.trim())
 .find((chunk) => chunk.startsWith(`${name}=`));
 if (!cookiePart) return '';
 return decodeURIComponent(cookiePart.substring(name.length + 1));
 } catch {
 return '';
 }
 };

 const getStableAuth = async () => {
 let lastError = null;

 for (let attempt = 0; attempt < 5; attempt += 1) {
 try {
 const { data: { session }, error } = await supabase.auth.getSession();
 if (session?.user) {
 return { session, user: session.user, error: null };
 }
 if (error) lastError = error;
 } catch (error) {
 lastError = error;
 }
 await wait(120);
 }

 const tokenFromCookie = getCookieValue('sb-access-token');
 if (tokenFromCookie) {
 try {
 const { data: { user }, error } = await supabase.auth.getUser(tokenFromCookie);
 if (user) return { session: null, user, error: null };
 if (error) lastError = error;
 } catch (error) {
 lastError = error;
 }
 }

 try {
 const { data: { user }, error } = await supabase.auth.getUser();
 if (user) return { session: null, user, error: null };
 if (error) lastError = error;
 } catch (error) {
 lastError = error;
 }

 return { session: null, user: null, error: lastError };
 };
 
 const avatarContainer = document.getElementById('avatar-container');
 const profileHeroBackdrop = document.getElementById('profile-hero-backdrop');
 const profileHeroBackdropImage = document.getElementById('profile-hero-backdrop-image') as HTMLImageElement | null;
 const uiUserName = document.getElementById('ui-user-name');
 const uiUserEmail = document.getElementById('ui-user-email');
 const uiRolesContainer = document.getElementById('ui-roles-container');
 const inputNombre = document.getElementById('input-nombre') as HTMLInputElement;
 const inputCorreo = document.getElementById('input-correo') as HTMLInputElement;
 
 const formDatosPersonales = document.getElementById('form-datos-personales') as HTMLFormElement;
 const selectTonalidad = document.getElementById('tonalidad-select') as HTMLSelectElement;
 const inputCumpleanos = document.getElementById('input-cumpleanos') as HTMLInputElement;
 const inputTelefono = document.getElementById('input-telefono') as HTMLInputElement;
 const btnSaveDatos = document.getElementById('btn-save-datos') as HTMLButtonElement;
 const profileFeedback = document.getElementById('profile-feedback') as HTMLElement | null;
 const profileFeedbackMessage = document.getElementById('profile-feedback-message');
 const profileFeedbackClose = document.getElementById('profile-feedback-close');
 let profileFeedbackTimer = 0;

 const hideProfileFeedback = () => {
  if (!profileFeedback || profileFeedback.hidden) return;
  profileFeedback.dataset.visible = 'false';
  window.clearTimeout(profileFeedbackTimer);
  profileFeedbackTimer = window.setTimeout(() => {
   profileFeedback.hidden = true;
  }, 220);
 };

 const showProfileFeedback = (tone: 'success' | 'error' | 'info', message: string) => {
  if (!profileFeedback || !profileFeedbackMessage) return;
  window.clearTimeout(profileFeedbackTimer);
  profileFeedback.dataset.tone = tone;
  profileFeedbackMessage.textContent = message;
  profileFeedback.hidden = false;
  requestAnimationFrame(() => {
   profileFeedback.dataset.visible = 'true';
  });
  profileFeedbackTimer = window.setTimeout(hideProfileFeedback, tone === 'error' ? 6200 : 4600);
 };

 on(profileFeedbackClose, 'click', hideProfileFeedback);

 const profileWizardNavItems = Array.from(document.querySelectorAll<HTMLButtonElement>('[data-profile-step]'));
 const profileWizardPanels = Array.from(document.querySelectorAll<HTMLElement>('[data-profile-panel]'));
 const profileWizardNav = document.querySelector<HTMLElement>('.profile-wizard-nav');
 const profileWizardStage = document.getElementById('profile-wizard-stage');
 const mobileProfileMedia = window.matchMedia('(max-width: 639px)');
 const reducedMotionMedia = window.matchMedia('(prefers-reduced-motion: reduce)');
 let currentProfileStep = 0;

 const syncProfileParallax = () => {
  (window as any).__perfilParallaxFrame = 0;
  if (!profileHeroBackdrop || !mobileProfileMedia.matches || reducedMotionMedia.matches) {
   profileHeroBackdrop?.style.removeProperty('--profile-parallax-y');
   return;
  }

  const hero = document.querySelector<HTMLElement>('.profile-wizard-hero');
  if (!hero) return;
  const scrollProgress = Math.min(hero.offsetHeight, Math.max(0, window.scrollY - hero.offsetTop));
  profileHeroBackdrop.style.setProperty('--profile-parallax-y', `${(scrollProgress * 0.055).toFixed(2)}px`);
 };

 const queueProfileParallax = () => {
  if ((window as any).__perfilParallaxFrame) return;
  (window as any).__perfilParallaxFrame = requestAnimationFrame(syncProfileParallax);
 };

 on(window, 'scroll', queueProfileParallax, { passive: true });
 on(window, 'resize', queueProfileParallax, { passive: true });
 on(mobileProfileMedia, 'change', queueProfileParallax);
 on(reducedMotionMedia, 'change', queueProfileParallax);
 queueProfileParallax();

 const syncProfileHeroBackdrop = (avatarUrl: string | null | undefined) => {
 const safeAvatarUrl = avatarUrl ? toSafeMediaUrl(avatarUrl) : '';
 if (!profileHeroBackdropImage || !profileHeroBackdrop) return;

 if (!safeAvatarUrl) {
 profileHeroBackdropImage.removeAttribute('src');
 profileHeroBackdrop.classList.remove('has-image');
 return;
 }

 profileHeroBackdropImage.src = safeAvatarUrl;
 profileHeroBackdrop.classList.add('has-image');
 };

 const setProfileStep = (requestedStep: number, focusTab = false) => {
 const nextStep = Math.max(0, Math.min(profileWizardPanels.length - 1, requestedStep));
 currentProfileStep = nextStep;

 profileWizardPanels.forEach((panel) => {
 const panelStep = Number(panel.dataset.profilePanel);
 const state = panelStep === nextStep ? 'active' : panelStep < nextStep ? 'before' : 'after';
 const isActive = state === 'active';
 panel.dataset.state = state;
 panel.setAttribute('aria-hidden', isActive ? 'false' : 'true');
 panel.toggleAttribute('inert', !isActive);
 if (isActive) panel.scrollTop = 0;
 });

 profileWizardNavItems.forEach((item) => {
 const isActive = Number(item.dataset.profileStep) === nextStep;
 item.setAttribute('aria-selected', isActive ? 'true' : 'false');
 item.tabIndex = isActive ? 0 : -1;
 if (focusTab && isActive) item.focus();
 });

 if (focusTab && mobileProfileMedia.matches && profileWizardNav && profileWizardStage) {
  requestAnimationFrame(() => {
   const navRect = profileWizardNav.getBoundingClientRect();
   if (navRect.top > 2) return;
   const stageTop = profileWizardStage.getBoundingClientRect().top + window.scrollY;
   const targetTop = Math.max(0, stageTop - profileWizardNav.offsetHeight);
   window.scrollTo({
    top: targetTop,
    behavior: reducedMotionMedia.matches ? 'auto' : 'smooth',
   });
  });
 }

 };

 profileWizardNavItems.forEach((item) => {
 on(item, 'click', () => setProfileStep(Number(item.dataset.profileStep), true));
 on(item, 'keydown', (event: Event) => {
 const keyboardEvent = event as KeyboardEvent;
 if (!['ArrowLeft', 'ArrowRight', 'Home', 'End'].includes(keyboardEvent.key)) return;
 keyboardEvent.preventDefault();
 if (keyboardEvent.key === 'Home') return setProfileStep(0, true);
 if (keyboardEvent.key === 'End') return setProfileStep(profileWizardPanels.length - 1, true);
 const direction = keyboardEvent.key === 'ArrowRight' ? 1 : -1;
 setProfileStep((currentProfileStep + direction + profileWizardPanels.length) % profileWizardPanels.length, true);
 });
 });

 setProfileStep(0);

 // ==========================================
 // LÃ³gica de Cropper y Upload de Foto de Perfil
 // ==========================================
 const btnChangeAvatar = document.getElementById('btn-change-avatar') as HTMLButtonElement | null;
 const avatarInput = document.getElementById('avatar-input') as HTMLInputElement;
 const cropperModal = document.getElementById('cropper-modal');
 const cropperContent = document.getElementById('cropper-content');
 const cropperImage = document.getElementById('cropper-image') as HTMLImageElement;
 const btnCloseCropper = document.getElementById('btn-close-cropper');
 const btnCancelCropper = document.getElementById('btn-cancel-cropper');
 const btnSaveCropper = document.getElementById('btn-save-cropper') as HTMLButtonElement;
 const avatarLockModal = document.getElementById('avatar-lock-modal');
 const avatarLockContent = document.getElementById('avatar-lock-content');
 const btnCloseAvatarLock = document.getElementById('btn-close-avatar-lock');
 const btnConfirmAvatarLock = document.getElementById('btn-confirm-avatar-lock');
 
 let canChangeAvatar = false;
 let isPreparingAvatar = false;

 const syncAvatarControls = () => {
 if (btnChangeAvatar) {
 btnChangeAvatar.disabled = !canChangeAvatar || isPreparingAvatar;
 btnChangeAvatar.classList.toggle('opacity-70', isPreparingAvatar);
 btnChangeAvatar.setAttribute('aria-busy', isPreparingAvatar ? 'true' : 'false');
 }
 if (avatarInput) {
 avatarInput.disabled = !canChangeAvatar || isPreparingAvatar;
 }
 };

 const setAvatarChangeState = (isAllowed: boolean) => {
 canChangeAvatar = isAllowed;
 syncAvatarControls();
 };

 const setAvatarProcessingState = (isBusy: boolean) => {
 isPreparingAvatar = isBusy;
 syncAvatarControls();
 };

 const supportsCanvasMimeType = (mimeType: string): Promise<boolean> => {
 return new Promise((resolve) => {
 const testCanvas = document.createElement('canvas');
 testCanvas.width = 1;
 testCanvas.height = 1;
 testCanvas.toBlob((blob) => resolve(Boolean(blob && blob.type === mimeType)), mimeType, 0.6);
 });
 };

 syncAvatarControls();

 const openAvatarLockModal = () => {
 avatarLockModal?.classList.remove('hidden');
 avatarLockModal?.classList.add('flex');
 requestAnimationFrame(() => {
 avatarLockModal?.classList.remove('opacity-0');
 avatarLockContent?.classList.remove('scale-95');
 });
 };

 const closeAvatarLockModal = () => {
 avatarLockModal?.classList.add('opacity-0');
 avatarLockContent?.classList.add('scale-95');
 setTimeout(() => {
 avatarLockModal?.classList.add('hidden');
 avatarLockModal?.classList.remove('flex');
 }, 250);
 };

 let cropper: any = (window as any)._cropperInstance || null;
 if (cropper) {
 try {
 cropper.destroy();
 } catch {
 // no-op
 }
 cropper = null;
 (window as any)._cropperInstance = null;
 }

 // Abrir selector de archivos
 on(btnChangeAvatar, 'click', () => {
 if (!canChangeAvatar) {
 openAvatarLockModal();
 return;
 }
 avatarInput?.click();
 });

 // Cuando el usuario selecciona una imagen
 on(avatarInput, 'change', async (e: Event) => {
 if (!canChangeAvatar) return;
 const file = (e.target as HTMLInputElement).files?.[0];
 // Reset input so the same file can be selected again if needed
 avatarInput.value = '';

 if (!file) return;

 setAvatarProcessingState(true);

 try {
 const { url: safeImageUrl } = await getSafeImageForCropper(file, 1000);
 revokePerfilSafeCropperUrl();
 (window as any).__perfilSafeCropperUrl = safeImageUrl;

 if (cropperImage) {
 const waitForImageLoad = new Promise<void>((resolve, reject) => {
 const cleanupImageListeners = () => {
 cropperImage.onload = null;
 cropperImage.onerror = null;
 };

 cropperImage.onload = () => {
 cleanupImageListeners();
 resolve();
 };

 cropperImage.onerror = () => {
 cleanupImageListeners();
 reject(new Error('No se pudo preparar la imagen para el recorte.'));
 };
 });

 cropperImage.src = safeImageUrl;
 cropperImage.classList.remove('hidden');
 if (!cropperImage.complete) {
 await waitForImageLoad;
 }
 openCropperModal();
 }
 } catch (error) {
 console.error('Error al preparar la imagen para el cropper:', error);
 alert(error instanceof Error ? error.message : 'Hubo un problema al cargar la imagen. Intenta con otra más pequeña.');
 } finally {
 setAvatarProcessingState(false);
 }
 });

 const openCropperModal = () => {
 if (!cropperImage) return;
 
 cropperModal?.classList.remove('hidden');
 cropperModal?.classList.add('flex');
 
 // Timeout para permitir que el modal se muestre antes de inicializar cropper y animar
 setTimeout(() => {
 cropperModal?.classList.remove('opacity-0');
 cropperContent?.classList.remove('scale-95');
 
 if (cropper) {
 cropper.destroy();
 }
 
 // Initialize Cropper via CDN (window.Cropper)
 const CropperJS = (window as any).Cropper;
 if(CropperJS) {
 cropper = new CropperJS(cropperImage, {
 aspectRatio: 1,
 viewMode: 1,
 dragMode: 'move',
 autoCropArea: 1,
 restore: false,
 guides: false,
 center: false,
 highlight: false,
 cropBoxMovable: true,
 cropBoxResizable: true,
 toggleDragModeOnDblclick: false,
 });
 (window as any)._cropperInstance = cropper;
 } else {
 alert('Error cargando herramienta de recorte. Intente recargar la página.');
 closeCropperModal();
 }
 }, 10);
 };

 const closeCropperModal = () => {
 cropperModal?.classList.add('opacity-0');
 cropperContent?.classList.add('scale-95');
 
 setTimeout(() => {
 cropperModal?.classList.add('hidden');
 cropperModal?.classList.remove('flex');
 if (cropper) {
 cropper.destroy();
 cropper = null;
 (window as any)._cropperInstance = null;
 }
 if (cropperImage) {
 cropperImage.src = '';
 cropperImage.classList.add('hidden');
 }
 revokePerfilSafeCropperUrl();
 }, 300);
 };

 on(btnCloseCropper, 'click', closeCropperModal);
 on(btnCancelCropper, 'click', closeCropperModal);
 on(btnCloseAvatarLock, 'click', closeAvatarLockModal);
 on(btnConfirmAvatarLock, 'click', closeAvatarLockModal);
 
 // Cerrar al hacer clic fuera
 on(cropperModal, 'click', (e: Event) => {
 if (e.target === cropperModal) closeCropperModal();
 });
 on(avatarLockModal, 'click', (e: Event) => {
 if (e.target === avatarLockModal) closeAvatarLockModal();
 });

 // Guardar recorte y subir a Supabase
 on(btnSaveCropper, 'click', async () => {
 if (!canChangeAvatar) return;
 if (!cropper) return;
 
 const originalText = btnSaveCropper.innerHTML;
 btnSaveCropper.innerHTML = '<svg class="animate-spin -ml-1 mr-2 h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg> Subiendo...';
 btnSaveCropper.disabled = true;
 setAvatarProcessingState(true);

 try {
 const useWebP = await supportsCanvasMimeType('image/webp');
 const outputMimeType = useWebP ? 'image/webp' : 'image/jpeg';
 const outputExtension = useWebP ? 'webp' : 'jpg';

 // Generar blob seguro de 400x400
 const canvas = cropper.getCroppedCanvas({
 width: 400,
 height: 400,
 imageSmoothingEnabled: true,
 imageSmoothingQuality: 'high',
 });

 const blob = await new Promise<Blob | null>((resolve) => {
 canvas.toBlob((b: Blob | null) => resolve(b), outputMimeType, useWebP ? 0.9 : 0.88);
 });

 if (!blob) throw new Error("No se pudo generar el recorte de la imagen.");

 const auth = await getStableAuth();
 const userId = currentUserId || auth.user?.id;
 if (!userId) throw new Error("No session");

 const fileName = `avatar-${Date.now()}.${outputExtension}`;
 const { publicUrl } = await uploadAvatarAtomic(userId, blob, fileName, {
 previousAvatarUrl: currentAvatarUrl,
 contentType: outputMimeType,
 });
 currentAvatarUrl = publicUrl;

 // Actualizar DOM visualmente
 const safePublicUrl = toSafeMediaUrl(publicUrl);
 const avatarContainerToUpdate = document.getElementById('avatar-container');
 if (avatarContainerToUpdate) {
 avatarContainerToUpdate.classList.remove('bg-brand/10');
 avatarContainerToUpdate.innerHTML = safePublicUrl
 ? safeHtml`<img src="${safePublicUrl}" alt="Avatar" crossorigin="anonymous" class="w-full h-full object-cover" id="avatar-preview" />`
 : '';
 }
 syncProfileHeroBackdrop(safePublicUrl);

 closeCropperModal();
 } catch (err) {
 console.error("Error al subir avatar:", err);
 alert(err instanceof Error ? err.message : 'Error al subir imagen. Intenta de nuevo.');
 } finally {
 btnSaveCropper.innerHTML = originalText;
 btnSaveCropper.disabled = false;
 setAvatarProcessingState(false);
 }
 });
 // ==========================================
 // LÃ³gica FASE 3: Ausencias
 // ==========================================
 const formAusencia = document.getElementById('form-ausencia') as HTMLFormElement;
 const btnSaveAusencia = document.getElementById('btn-save-ausencia') as HTMLButtonElement;
 const inputInicio = document.getElementById('ausencia-inicio') as HTMLInputElement;
 const inputFin = document.getElementById('ausencia-fin') as HTMLInputElement;
 const inputMotivo = document.getElementById('ausencia-motivo') as HTMLInputElement;
 const absenceDateFormatter = new Intl.DateTimeFormat('en-CA', {
 timeZone: 'America/Bogota',
 year: 'numeric',
 month: '2-digit',
 day: '2-digit',
 });
 const addDaysToDateOnly = (dateOnly: string, amount: number) => {
 const anchor = new Date(`${dateOnly}T12:00:00Z`);
 anchor.setUTCDate(anchor.getUTCDate() + amount);
 return anchor.toISOString().slice(0, 10);
 };
 const absenceMinStartDate = addDaysToDateOnly(absenceDateFormatter.format(new Date()), 7);
 const syncAbsenceDateConstraints = ({ forceResetEnd = false } = {}) => {
 const nextMinEndDate = inputInicio?.value && inputInicio.value > absenceMinStartDate
 ? inputInicio.value
 : absenceMinStartDate;
 if (inputInicio) inputInicio.min = absenceMinStartDate;
 if (inputFin) inputFin.min = nextMinEndDate;
 if (inputFin && (forceResetEnd || !inputFin.value || inputFin.value < nextMinEndDate)) {
 inputFin.value = nextMinEndDate;
 }
 };

 syncAbsenceDateConstraints({ forceResetEnd: true });
 on(inputInicio, 'input', () => syncAbsenceDateConstraints({ forceResetEnd: true }));
 on(inputInicio, 'change', () => syncAbsenceDateConstraints({ forceResetEnd: true }));

 on(formAusencia, 'submit', async (e: Event) => {
 e.preventDefault();
 if (!inputInicio.value || !inputFin.value) return;

 const originalBtnText = btnSaveAusencia.textContent;
 btnSaveAusencia.textContent = 'Guardando...';
 btnSaveAusencia.disabled = true;

 try {
 const auth = await getStableAuth();
 const userId = currentUserId || auth.user?.id;
 if (!userId) throw new Error("No session");

 const response = await fetch('/api/absences', {
 method: 'POST',
 credentials: 'same-origin',
 headers: {
 'content-type': 'application/json'
 },
 body: JSON.stringify({
 fecha_inicio: inputInicio.value,
 fecha_fin: inputFin.value,
 motivo: inputMotivo.value || null
 })
 });
 const payload = await response.json().catch(() => ({}));
 if (!response.ok || payload?.ok === false) {
 throw new Error(payload?.error || 'No se pudo registrar la ausencia.');
 }

 const result = payload?.result || {};
 const summaryMessages = ['Ausencia registrada correctamente.'];
 const removedAssignments = Number(result?.removedAssignments || 0);
 const leadersNotified = Number(result?.leadersNotified || 0);
 const eventsWithoutLeader = Number(result?.eventsWithoutLeader || 0);

 if (removedAssignments > 0) {
 try {
 await fetch('/api/calendar/google/sync', {
 method: 'POST',
 credentials: 'same-origin',
 headers: { 'content-type': 'application/json' },
 body: '{}'
 });
 } catch (calendarSyncError) {
 console.warn('Google Calendar sync after absence failed:', calendarSyncError);
 }
 }

 if (removedAssignments > 0) {
 const label = removedAssignments === 1 ? 'servicio' : 'servicios';
 summaryMessages.push(`Te retiramos de ${removedAssignments} ${label}.`);
 }

 if (leadersNotified > 0) {
 const label = leadersNotified === 1 ? 'lider' : 'lideres';
 summaryMessages.push(`Avisamos a ${leadersNotified} ${label}.`);
 }

 if (eventsWithoutLeader > 0) {
 const label = eventsWithoutLeader === 1 ? 'servicio' : 'servicios';
 summaryMessages.push(`Hay ${eventsWithoutLeader} ${label} sin liderazgo asignado para avisar.`);
 }

 alert(summaryMessages.join(' '));
 
 // Recargar pÃ¡gina para actualizar lista
 window.location.reload();
} catch (err) {
 console.error('Error insertando ausencia:', err);
 alert(err instanceof Error ? err.message : 'Hubo un error al registrar la ausencia. Verifica las fechas.');
 btnSaveAusencia.textContent = originalBtnText;
 btnSaveAusencia.disabled = false;
 }
 });

 // Borrar Ausencia
 document.querySelectorAll('.btn-delete-ausencia').forEach((btn) => {
 on(btn, 'click', async (e: Event) => {
 const button = e.currentTarget as HTMLButtonElement;
 const id = button.getAttribute('data-id');
 if (!id) return;
 
 if (!confirm('\u00BFEst\u00E1s seguro de eliminar este bloqueo de fechas?')) return;
 
 button.disabled = true;
 button.classList.add('opacity-50');

 try {
 const { error } = await supabase
 .from('ausencias')
 .delete()
 .eq('id', id);

 if (error) throw error;
 
 // Remover del DOM visualmente
 const row = document.querySelector(`[data-ausencia-id="${id}"]`);
 row?.remove();

 } catch (err) {
 console.error('Error eliminando ausencia:', err);
 alert("Error eliminando registro.");
 button.disabled = false;
 button.classList.remove('opacity-50');
 }
 });
 });

 // ==========================================
 // Lógica FASE 4: Seguridad
 // ==========================================
 const clearClientAuthState = () => {
 try {
 if (window.__REDIL_AUTH_MANAGER__?.clearSessionCookies) {
 window.__REDIL_AUTH_MANAGER__.clearSessionCookies();
 return;
 }
 } catch {
 // no-op
 }

 const secureDeleteFlag = window.location.protocol === 'https:' ? '; Secure' : '';
 const localNames = new Set(['sb-access-token', 'sb-refresh-token']);
 const rawCookies = document.cookie ? document.cookie.split(';') : [];
 rawCookies.forEach((cookiePart) => {
 const [rawName] = cookiePart.trim().split('=');
 if (!rawName) return;
 if (/^(sb-|__Host-sb-|__Secure-sb-|supabase-)/i.test(rawName)) {
 localNames.add(rawName);
 }
 });

 localNames.forEach((name) => {
 document.cookie = `${name}=; path=/; max-age=0; expires=Thu, 01 Jan 1970 00:00:00 UTC; SameSite=Lax${secureDeleteFlag}`;
 document.cookie = `${name}=; path=/; max-age=0; expires=Thu, 01 Jan 1970 00:00:00 UTC; SameSite=None${secureDeleteFlag}`;
 });
 };

 const btnLogout = document.getElementById('btn-logout');
 on(btnLogout, 'click', async () => {
 if (!confirm('\u00BFSeguro que deseas cerrar la sesi\u00F3n?')) return;

 try {
 const { error } = await supabase.auth.signOut({ scope: 'global' });
 if (error) throw error;
 } catch (logoutError) {
 console.error('Logout Error (supabase):', logoutError);
 } finally {
 clearClientAuthState();
 try {
 await fetch('/api/auth/logout', {
 method: 'POST',
 credentials: 'include',
 headers: {
 'cache-control': 'no-store',
 },
 });
 } catch (cookieError) {
 console.error('Logout Error (cookie endpoint):', cookieError);
 }
 window.location.replace('/login');
 }
 });

 const btnChangePassword = document.getElementById('btn-change-password') as HTMLButtonElement;
 on(btnChangePassword, 'click', async () => {
   const { user } = await getStableAuth();
   if (!user?.email) { alert('No se pudo obtener tu email. Recarga la página.'); return; }
   btnChangePassword.disabled = true;
   btnChangePassword.textContent = 'Enviando…';
   const { error } = await supabase.auth.resetPasswordForEmail(user.email, {
     redirectTo: `${window.location.origin}/reset-password`,
   });
   btnChangePassword.disabled = false;
   btnChangePassword.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 2l-2 2m-7.61 7.61a5.5 5.5 0 1 1-7.778 7.778 5.5 5.5 0 0 1 7.777-7.777zm0 0L15.5 7.5m0 0l3 3L22 7l-3-3m-3.5 3.5L19 4"></path></svg> Cambiar Contraseña`;
   if (error) {
     alert('Error: ' + error.message);
   } else {
     alert('✅ Correo enviado a ' + user.email + '. Revisa tu bandeja y sigue el enlace.');
   }
 });

 // ==========================================
 // CLIENT-SIDE DATA FETCHING
 // ==========================================
 const fetchData = async () => {
 const response = await fetch('/api/profile-data', {
 credentials: 'same-origin',
 cache: 'no-store',
 headers: { accept: 'application/json' },
 });
 const payload = await response.json().catch(() => ({}));

 if (response.status === 401) {
 window.location.replace('/login');
 return;
 }
 if (!response.ok || !payload?.user?.id) {
 console.warn('Perfil: no se pudieron cargar los datos desde el servidor', payload?.error);
 return;
 }

 currentUserId = payload.user.id;
 const email = payload.user.email || '';
 
 if (inputCorreo) inputCorreo.value = email || '';
 if (inputCorreo) {
  inputCorreo.disabled = false;
  (window as any)._initialEmail = email || '';
 }
 if (uiUserEmail) uiUserEmail.textContent = email || '';

 // La sesión del servidor también funciona en la web app instalada de iOS,
 // aunque su almacenamiento local de Supabase esté vacío o haya caducado.
 const perfilData = payload.profile;

 if (perfilData) {
 setAvatarChangeState(perfilData.can_change_avatar !== false);
 if (inputNombre) inputNombre.value = perfilData.nombre;
 
 if (uiUserName) {
 uiUserName.classList.remove('bg-neutral/20', 'bg-white/15', 'animate-pulse', 'h-7', 'h-8', 'min-w-[120px]', 'min-w-[140px]', 'flex', 'items-center', 'justify-center');
 uiUserName.textContent = perfilData.nombre;
 }
 
 if (uiUserEmail) {
 uiUserEmail.classList.remove('bg-neutral/20', 'bg-white/10', 'animate-pulse', 'h-5', 'min-w-[160px]', 'min-w-[180px]', 'flex', 'items-center', 'justify-center');
 uiUserEmail.textContent = email || '';
 }

 // Set vocal key and birthday
 if(selectTonalidad) {
 selectTonalidad.disabled = false;
 if (perfilData.tonalidad_voz) {
 selectTonalidad.value = perfilData.tonalidad_voz;
 (window as any)._initialTonalidad = perfilData.tonalidad_voz;
 }
 }
 if (inputCumpleanos) {
 inputCumpleanos.disabled = false;
 if (perfilData.fecha_nacimiento) {
 inputCumpleanos.value = perfilData.fecha_nacimiento;
 (window as any)._initialCumpleanos = perfilData.fecha_nacimiento;
 }
 }
 if (inputTelefono) {
 inputTelefono.disabled = false;
 if (perfilData.telefono) {
 inputTelefono.value = perfilData.telefono;
 (window as any)._initialTelefono = perfilData.telefono;
 }
 }
 if(avatarContainer) {
 if (perfilData.avatar_url) {
 currentAvatarUrl = perfilData.avatar_url;
 avatarContainer.classList.remove('bg-brand/10');
 const safeAvatarUrl = toSafeMediaUrl(perfilData.avatar_url);
 avatarContainer.innerHTML = safeAvatarUrl
 ? safeHtml`<img src="${safeAvatarUrl}" alt="${perfilData.nombre}" crossorigin="anonymous" class="w-full h-full object-cover" id="avatar-preview" />`
 : '';
 syncProfileHeroBackdrop(safeAvatarUrl);
 } else {
 currentAvatarUrl = null;
 const initials = perfilData.nombre.split(' ').map((n: string) => n[0]).join('').substring(0,2).toUpperCase();
 avatarContainer.innerHTML = safeHtml`<span id="avatar-preview-text" class="text-brand">${initials}</span>`;
 avatarContainer.classList.add('bg-brand/10');
 syncProfileHeroBackdrop(null);
 }
 }
 } else {
 currentAvatarUrl = null;
 syncProfileHeroBackdrop(null);
 setAvatarChangeState(true);
 }

 formAusencia?.classList.remove('opacity-50', 'pointer-events-none');

 const rolesData = Array.isArray(payload.roles) ? payload.roles : [];

 if (uiRolesContainer) {
 const userRoles = rolesData ? rolesData.map((r: any) => r.roles?.nombre).filter(Boolean) : [];
 uiRolesContainer.innerHTML = '';
 
 if (userRoles.length === 0) {
 uiRolesContainer.innerHTML = '<span class="text-xs text-content-muted italic">Sin roles asignados</span>';
 } else {
 userRoles.forEach((rol: string) => {
 uiRolesContainer.insertAdjacentHTML('beforeend', safeHtml`<span class="px-2.5 py-1 text-xs font-bold uppercase tracking-wider rounded-md bg-background border border-border text-content-muted shadow-sm">${rol}</span>`);
 });
 }
 }

 // 3. Fetch Ausencias
 const ausenciasListEl = document.getElementById('ausencias-list');
 const ausenciasData = Array.isArray(payload.absences) ? payload.absences : [];

 if (ausenciasListEl) {
 const futureAusencias = ausenciasData || [];
 ausenciasListEl.innerHTML = '';
 
 if (futureAusencias.length === 0) {
 ausenciasListEl.innerHTML = `
 <div class="py-6 text-center border-2 border-dashed border-border rounded-xl">
 <p class="text-xs font-bold text-content-muted">No tienes ausencias registradas.</p>
 </div>
 `;
 } else {
 futureAusencias.forEach((aus: any) => {
 const inicio = new Date(aus.fecha_inicio).toLocaleDateString('es', { day: 'numeric', month: 'short', timeZone: 'UTC' });
 const fin = new Date(aus.fecha_fin).toLocaleDateString('es', { day: 'numeric', month: 'short', timeZone: 'UTC' });
 const motivoHtml = aus.motivo ? `<p class="text-xs text-content-muted">${aus.motivo}</p>` : '';
 
 // We recreate the HTML for each item
 const div = document.createElement('div');
 div.className = "flex items-center justify-between p-3 rounded-lg border border-neutral-100 bg-background group shrink-0 transition-opacity";
 div.setAttribute('data-ausencia-id', aus.id);
 div.innerHTML = safeHtml`
 <div>
 <p class="text-sm font-bold text-content ">
 ${inicio} <span class="text-content-muted mx-1">-</span> ${fin}
 </p>
 ${motivoHtml}
 </div>
 <button type="button" class="p-2 rounded-lg text-content-muted hover:text-red-500 hover:bg-red-50 dark:bg-red-950/30 transition-colors" title="Eliminar">
 <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"></path><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"></path><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"></path></svg>
 </button>
 `;
 const deleteAbsenceButton = div.querySelector('button');
 on(deleteAbsenceButton, 'click', () => (window as any)._deleteAusencia(String(aus.id)));
 ausenciasListEl.appendChild(div);
 });
 }

 if (futureAusencias.length > 0) {
 try {
 const reconcileResponse = await fetch('/api/absences/reconcile', {
 method: 'POST',
 credentials: 'same-origin',
 });
 const reconcilePayload = await reconcileResponse.json().catch(() => ({}));

 if (reconcileResponse.ok && reconcilePayload?.ok) {
 const removedAssignments = Number(reconcilePayload?.result?.removedAssignments || 0);
 const cleanedVoiceSlots = Number(reconcilePayload?.result?.cleanedVoiceSlots || 0);

 if (removedAssignments > 0 || cleanedVoiceSlots > 0) {
 const fixes = [];
 if (removedAssignments > 0) {
 const label = removedAssignments === 1 ? 'servicio' : 'servicios';
 fixes.push(`${removedAssignments} ${label}`);
 }
 if (cleanedVoiceSlots > 0) {
 const label = cleanedVoiceSlots === 1 ? 'asignacion vocal' : 'asignaciones vocales';
 fixes.push(`${cleanedVoiceSlots} ${label}`);
 }
 alert(`Corregimos automaticamente conflictos viejos con tus ausencias: ${fixes.join(' y ')}.`);
 }
 } else if (reconcilePayload?.error) {
 console.warn('Absence reconciliation warning:', reconcilePayload.error);
 }
 } catch (reconcileError) {
 console.warn('Absence reconciliation request failed:', reconcileError);
 }
 }
 }
 };

 // Initialize fetch
 fetchData();

 // -------------------------------------------------------------------------
 // Manejar envÃ­o de Formulario de Datos Personales
 // -------------------------------------------------------------------------

 const checkProfileChanges = () => {
 if (!selectTonalidad || !inputCumpleanos || !inputTelefono || !inputCorreo || !btnSaveDatos) return;
 const btnHtml = btnSaveDatos.innerHTML;
 if(btnHtml.includes('Guardando')) return; // ignore during save

 const tVal = selectTonalidad.value || '';
 const cVal = inputCumpleanos.value || '';
 const phoneVal = inputTelefono.value || '';
 const emailVal = inputCorreo.value.trim().toLowerCase();
 const initT = (window as any)._initialTonalidad || '';
 const initC = (window as any)._initialCumpleanos || '';
 const initPhone = (window as any)._initialTelefono || '';
 const initEmail = String((window as any)._initialEmail || '').trim().toLowerCase();
 
 if (tVal !== initT || cVal !== initC || phoneVal !== initPhone || emailVal !== initEmail) {
 btnSaveDatos.classList.remove('hidden');
 } else {
 btnSaveDatos.classList.add('hidden');
 }
 };

 on(selectTonalidad, 'input', checkProfileChanges);
 on(inputCumpleanos, 'input', checkProfileChanges);
 on(inputTelefono, 'input', checkProfileChanges);
 on(inputCorreo, 'input', checkProfileChanges);

 if (formDatosPersonales && btnSaveDatos) {
 on(formDatosPersonales, 'submit', async (e: Event) => {
 e.preventDefault();
 const vocalVal = selectTonalidad.value;
 const cumpleanosVal = inputCumpleanos.value;
 const telefonoVal = inputTelefono.value;
 const correoVal = inputCorreo.value.trim().toLowerCase();
 const initialEmail = String((window as any)._initialEmail || '').trim().toLowerCase();
 const emailChanged = correoVal !== initialEmail;

 if (!inputCorreo.checkValidity()) {
  inputCorreo.reportValidity();
  return;
 }

 const originalBtnHtml = btnSaveDatos.innerHTML;
 btnSaveDatos.innerHTML = '<span class="flex items-center gap-2"><div class="w-4 h-4 rounded-full border-2 border-white/20 border-t-white animate-spin"></div> Guardando...</span>';
 btnSaveDatos.disabled = true;

 try {
 const auth = await getStableAuth();
 const userId = currentUserId || auth.user?.id;
 if (!userId) throw new Error('No hay sesión activa');

 const updates = {
 fecha_nacimiento: cumpleanosVal || null,
 telefono: telefonoVal || null,
 updated_at: new Date().toISOString()
 };

 const [profileUpdate, vocalUpdate] = await Promise.all([
 supabase
 .from('perfiles')
 .update(updates)
 .eq('id', userId)
 .select('id'),
 supabase.rpc('set_my_vocal_range', {
 p_vocal_range: vocalVal || null,
 }),
 ]);

 const { data: updatedProfile, error } = profileUpdate;

 if (error) throw error;
 if (vocalUpdate.error) throw vocalUpdate.error;

 // Validación Anti-Fallo Silencioso RLS
 if (!updatedProfile || updatedProfile.length === 0) {
 console.error("Fallo silencioso RLS: Supabase bloqueó la actualización de datos.");
 alert("Error de permisos: No pudimos guardar tu información.");
 throw new Error("Violación de RLS al actualizar datos personales");
 }

 (window as any)._initialTonalidad = vocalVal;
 (window as any)._initialCumpleanos = cumpleanosVal;
 (window as any)._initialTelefono = telefonoVal;
 let feedbackMessage = 'Cambios guardados.';

 if (emailChanged) {
  const emailResponse = await fetch('/api/profile-email', {
   method: 'POST',
   credentials: 'same-origin',
   headers: { 'Content-Type': 'application/json' },
   body: JSON.stringify({ email: correoVal }),
  });
  const emailPayload = await emailResponse.json().catch(() => ({}));

  if (!emailResponse.ok) {
   showProfileFeedback('error', emailPayload?.error || 'Tus datos se guardaron, pero no pudimos iniciar el cambio de correo.');
   checkProfileChanges();
   return;
  }

  (window as any)._initialEmail = correoVal;
  feedbackMessage = emailPayload?.confirmationRequired
   ? 'Cambios guardados. Revisa tu correo para confirmar la nueva dirección.'
   : 'Cambios guardados. Tu correo de acceso fue actualizado.';
 }

 checkProfileChanges();
 showProfileFeedback('success', feedbackMessage);
 } catch (error) {
 console.error("Error al actualizar datos:", error);
 showProfileFeedback('error', 'No se pudieron guardar los cambios. Intenta nuevamente.');
 } finally {
 btnSaveDatos.innerHTML = originalBtnHtml;
 btnSaveDatos.disabled = false;
 checkProfileChanges();
 }
 });
 }
 
 // Expose delete function to window context for the innerHTML buttons
 (window as any)._deleteAusencia = async (id: string) => {
 if (!confirm('\u00BFEst\u00E1s seguro de eliminar este bloqueo de fechas?')) return;
 
 const row = document.querySelector(`[data-ausencia-id="${id}"]`) as HTMLElement;
 if(row) row.style.opacity = '0.5';

 try {
 const { error } = await supabase
 .from('ausencias')
 .delete()
 .eq('id', id);

 if (error) throw error;
 row?.remove();
 
 // If it was the last one, fetch again to show empty state
 if (document.querySelectorAll('[data-ausencia-id]').length === 0) {
 fetchData();
 }

 } catch (err) {
 console.error('Error eliminando ausencia:', err);
 alert("Error eliminando registro.");
 if(row) row.style.opacity = '1';
 }
 }

 };

 if ((window as any).__perfilPageLoadHandler) {
 document.removeEventListener('astro:page-load', (window as any).__perfilPageLoadHandler);
 }

 if ((window as any).__perfilAfterSwapHandler) {
 document.removeEventListener('astro:after-swap', (window as any).__perfilAfterSwapHandler);
 }

 const onPerfilAfterSwap = () => {
 cleanupPerfilRuntime();
 };

 (window as any).__perfilPageLoadHandler = initPerfilPage;
 (window as any).__perfilAfterSwapHandler = onPerfilAfterSwap;
 document.addEventListener('astro:page-load', initPerfilPage);
 document.addEventListener('astro:after-swap', onPerfilAfterSwap);
</script>
